import type { FC } from 'react';

import { Container } from '../container';

export const Header: FC = () => {
  return (
    <header>
      <Container className="pt-6 sm:py-12">Header</Container>
    </header>
  );
};
